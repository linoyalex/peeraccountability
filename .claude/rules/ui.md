---
paths: ["app/**/*.tsx", "components/**"]
---

- Tap targets ≥44px — this app is used one-handed, often sweaty, seconds after a workout.
- Primary controls (Prove It, Back it/Call it, Send it) sit in the lower half of the screen, reachable by thumb.
- No fake status bar or chrome — this is a PWA, not a native-app mockup.
- Copy comes from `docs/BUILD.md` §10 verbatim. Don't paraphrase or invent strings.
- Visual tokens (colors, type) come from `docs/BUILD.md` §11 — treat exact hex/font values there as directional, not final, per that section's own caveat.
